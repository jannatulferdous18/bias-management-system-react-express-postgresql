{
  "name": "bias-management-system",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "start:client": "npm --prefix client start",
    "start:server": "npm --prefix server start",
    "dev": "concurrently \"npm run start:client\" \"npm run start:server\""
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.49.4"
  }
}
